# Look for Buzz tools, libraries, and headers
find_package(Buzz REQUIRED)

# Define Buzz-related commands
#include(UseBuzz)

# Compile a script that does not include any other script
#buzz_make(general.bzz)
#buzz_make(worker.bzz)